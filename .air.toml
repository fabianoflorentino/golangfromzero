[build]
  root = "/app"
  bin = "/usr/local/bin/app"
  cmd = 'GOFLAGS="-trimpath" CGO_DISABLED=1 GOARCH=amd64 go build -ldflags="-s -w" -o /usr/local/bin/app /app/cmd/app'
  exclude_dir = ["assets", "tmp", "vendor", "testdata"]
  exclude_file = [".md"]
  exclude_regex = ["_test.go"]
  poll = true
  poll_interval = 60
